os: linux
language: python
python: pypy3.9-7.3.9
script: python -V

install: true

jobs:
  include:
    - dist: bionic
      arch: amd64
    - dist: focal
      arch: amd64
    - dist: jammy
      arch: amd64
